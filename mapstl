#include<bits/stdc++.h>
using namespace std;
int main()
{
	map<int,int>mp;//unordered_map<int,int>mp;
  /*unordered take less complextity than map bcs to sort */
	int arr[10]={1,2,3,4,5,2,3,1,4,3};
	for(int i=0;i<10;i++)
	mp[arr[i]]++;
	for(auto it:mp)
	cout<<it.first<<" "<<it.second<<"\n";
}
